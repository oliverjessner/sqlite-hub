1. Logik der cli extrahieren und die gleiche logik für eine api nutzen die wir jetzt entwicklen.
   In settings kann man einen token erstellen und tokens löschen, dieser token ist für die datenbank gültig in der ich ihn erstelle
