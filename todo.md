# AI Fix Todo

This file was generated by ItWorksBut. It is optimized for coding agents: work top to bottom, keep changes focused, and mark tasks complete only after verification.

## Agent Rules

- Fix critical and high findings before lower-priority cleanup.
- Inspect the referenced code before editing, especially for heuristic findings.
- Preserve existing behavior unless the finding explicitly requires changing it.
- Add or update focused tests when the change has behavioral risk.
- Do not silence ItWorksBut checks unless the underlying issue is actually fixed.
- Never print, log, or preserve raw secret values. Use placeholders only.

## Scan Summary

- Tool: ItWorksBut 0.5.0
- Project: /Users/oli/github/sqlite-hub
- Completed: 2026-05-12T13:30:12.158Z
- Files scanned: 138
- Text files scanned: 116
- Fail-on: low
- Exit decision: 1
- Total findings: 61
- Critical: 0
- High: 61
- Medium: 0
- Low: 0
- Info: 0

## High Findings

### 1. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `bin/sqlite-hub.js:337`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at bin/sqlite-hub.js:337. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 2. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/app.js:370`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/app.js:370. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 3. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/components/connectionCard.js:28`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/components/connectionCard.js:28. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 4. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/components/emptyState.js:23`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/components/emptyState.js:23. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 5. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/components/modal.js:100`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/components/modal.js:100. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 6. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/components/modal.js:295`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/components/modal.js:295. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 7. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/components/modal.js:380`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/components/modal.js:380. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 8. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/components/modal.js:422`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/components/modal.js:422. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 9. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/components/modal.js:696`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/components/modal.js:696. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 10. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/components/modal.js:729`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/components/modal.js:729. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 11. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/components/queryEditor.js:21`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/components/queryEditor.js:21. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 12. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/components/queryHistoryDetail.js:213`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/components/queryHistoryDetail.js:213. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 13. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/components/queryHistoryPanel.js:24`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/components/queryHistoryPanel.js:24. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 14. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/components/queryHistoryPanel.js:54`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/components/queryHistoryPanel.js:54. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 15. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/components/queryResults.js:79`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/components/queryResults.js:79. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 16. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/components/rowEditorPanel.js:178`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/components/rowEditorPanel.js:178. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 17. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/components/tableDesignerEditor.js:145`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/components/tableDesignerEditor.js:145. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 18. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/components/tableDesignerEditor.js:162`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/components/tableDesignerEditor.js:162. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 19. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/components/tableDesignerEditor.js:61`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/components/tableDesignerEditor.js:61. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 20. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/components/tableDesignerEditor.js:85`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/components/tableDesignerEditor.js:85. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 21. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/utils/tableDesigner.js:716`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/utils/tableDesigner.js:716. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 22. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/views/charts.js:377`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/views/charts.js:377. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 23. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/views/data.js:245`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/views/data.js:245. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 24. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/views/data.js:267`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/views/data.js:267. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 25. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/views/data.js:355`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/views/data.js:355. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 26. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/views/mediaTagging.js:187`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/views/mediaTagging.js:187. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 27. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/views/mediaTagging.js:304`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/views/mediaTagging.js:304. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 28. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/views/mediaTagging.js:637`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/views/mediaTagging.js:637. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 29. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/views/mediaTagging.js:667`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/views/mediaTagging.js:667. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 30. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/views/mediaTagging.js:682`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/views/mediaTagging.js:682. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 31. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `frontend/js/views/structure.js:18`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at frontend/js/views/structure.js:18. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 32. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/dataBrowserService.js:187`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/dataBrowserService.js:187. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 33. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/dataBrowserService.js:217`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/dataBrowserService.js:217. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 34. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/dataBrowserService.js:274`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/dataBrowserService.js:274. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 35. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/dataBrowserService.js:80`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/dataBrowserService.js:80. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 36. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/exportService.js:73`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/exportService.js:73. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 37. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/introspection.js:226`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/introspection.js:226. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 38. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/introspection.js:95`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/introspection.js:95. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 39. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/mediaTaggingService.js:1175`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/mediaTaggingService.js:1175. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 40. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/mediaTaggingService.js:1192`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/mediaTaggingService.js:1192. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 41. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/mediaTaggingService.js:1213`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/mediaTaggingService.js:1213. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 42. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/mediaTaggingService.js:1377`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/mediaTaggingService.js:1377. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 43. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/mediaTaggingService.js:1401`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/mediaTaggingService.js:1401. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 44. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/mediaTaggingService.js:1421`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/mediaTaggingService.js:1421. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 45. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/mediaTaggingService.js:1474`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/mediaTaggingService.js:1474. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 46. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/mediaTaggingService.js:1480`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/mediaTaggingService.js:1480. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 47. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/mediaTaggingService.js:1596`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/mediaTaggingService.js:1596. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 48. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/mediaTaggingService.js:1685`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/mediaTaggingService.js:1685. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 49. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/mediaTaggingService.js:1702`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/mediaTaggingService.js:1702. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 50. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/mediaTaggingService.js:1719`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/mediaTaggingService.js:1719. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 51. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/mediaTaggingService.js:318`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/mediaTaggingService.js:318. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 52. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/mediaTaggingService.js:345`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/mediaTaggingService.js:345. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 53. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/mediaTaggingService.js:353`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/mediaTaggingService.js:353. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 54. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/mediaTaggingService.js:966`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/mediaTaggingService.js:966. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 55. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/structureService.js:57`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/structureService.js:57. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 56. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/sqlite/tableDesigner/sql.js:54`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/sqlite/tableDesigner/sql.js:54. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 57. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/storage/appStateStore.js:1012`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/storage/appStateStore.js:1012. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 58. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/storage/appStateStore.js:1044`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/storage/appStateStore.js:1044. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 59. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/storage/appStateStore.js:1607`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/storage/appStateStore.js:1607. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 60. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/storage/appStateStore.js:2282`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/storage/appStateStore.js:2282. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.

### 61. It works, but your SQL query is one template string away from pain.

- [ ] Fix this finding.
- Check ID: `database.raw-sql-interpolation`
- Severity: `high`
- Category: `database`
- Location: `server/services/storage/appStateStore.js:445`
- Heuristic: `yes`
- Tags: database, sql-injection
- Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation.
- Recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings.

#### Agent Prompt

```text
You are a senior security engineer working in this repository. Fix the ItWorksBut finding database.raw-sql-interpolation at server/services/storage/appStateStore.js:445. This finding is heuristic, so inspect the code first and only change behavior when the risk is real. Problem: Possible SQL injection risk: raw SQL appears to be built with template string interpolation. Required change: Replace SQL string interpolation or concatenation with parameterized queries, prepared statements, or a safe ORM query builder. Existing recommendation: Use parameterized queries, prepared statements, or ORM query builders instead of interpolating values into SQL strings. Keep existing behavior intact where possible, add or update focused tests when useful, and do not silence the check unless the underlying risk is actually fixed.
```

#### Acceptance Criteria

- [ ] The issue behind `database.raw-sql-interpolation` is fixed at the source.
- [ ] Existing behavior is preserved or intentional behavior changes are covered by tests.
- [ ] No raw secrets, tokens, credentials, or private values were added to code, logs, tests, or this todo.
- [ ] The relevant scanner finding no longer appears after rerunning ItWorksBut.


## Final Verification

- [ ] Run the relevant test suite.
- [ ] Run `itworksbut scan` again.
- [ ] Confirm there are no remaining findings at or above the configured fail-on severity.

